# Kimitsu releases

Notarized builds of [Kimitsu](https://kimitsu.lemonsqueezy.com) and the Sparkle appcast the app checks for updates.

`appcast.xml` is regenerated by `tools/release.sh` in the main repo (requires the Sparkle EdDSA key from the maintainer's keychain). Zips are attached to GitHub releases.
